# Local Asset Policy

The user confirmed authorization to reproduce Murdoku gameplay and assets for this mobile version.
Reference assets that are explicitly pulled from the live site are stored in `public/murdoku-assets`.

Use stable keys that match `portraitKey` and board object metadata in case data.
When a reference asset is not available in the public bundle, keep the local text/CSS fallback instead of guessing a file path.
